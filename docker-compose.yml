services:
  backend:
    container_name: isme-backend
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DATA_PATH: /data
    ports:
      - "${PORT:-8081}:8081"
    volumes:
      - ./data:/data
